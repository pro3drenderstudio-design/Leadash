/**
 * /extension — now renders the v2 page. The v2 path at /v2/extension continues to
 * render the same component so old preview links still resolve.
 */

export { default, metadata } from "../v2/extension/page";
