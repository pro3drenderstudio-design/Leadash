/**
 * /beta — now renders the v2 page. The v2 path at /v2/beta continues to
 * render the same component so old preview links still resolve.
 */

export { default, metadata } from "../v2/beta/page";
